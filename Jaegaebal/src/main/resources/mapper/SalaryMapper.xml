<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="kr.or.jaegaebal.mapper.SalaryMapper">
	<resultMap type="SalaryInfo" id="SalaryInfoResultMap">
		<result column="data_num" 		property="dataNum"/>
		<result column="data_emp" 		property="dataEmp"/>
		<result column="staff_name" 		property="staffName"/>
		<result column="level_name" 		property="levelName"/>
		<result column="human_num" 		property="humanNum"/>
		<result column="company_in_date" 		property="companyInDate"/>
		<result column="company_out_date" 		property="companyOutDate"/>
		<result column="staff_nationality" 		property="staffNationality"/>
		<result column="staff_country" 		property="staffCountry"/>
		<result column="staff_phone" 		property="staffPhone"/>
		<result column="staff_email" 		property="staffEmail"/>
		<result column="data_insu" 	property="dataInsu"/>
		<result column="data_resi" 	property="dataResi"/>
		<result column="data_pro" 	property="dataPro"/>
		<result column="data_bank" 	property="dataBank"/>
		<result column="data_account" property="dataAccount"/>
		<result column="data_host" property="dataHost"/>
		<result column="data_scrib" property="dataScrib"/>
		<result column="data_bate" property="dataBate"/>
		<result column="data_bate_per" property="dataBatePer"/>
		<result column="data_bate_date1" property="dataBateDate1"/>
		<result column="data_bate_date2" property="dataBateDate2"/>
		<result column="data_expen" property="dataExpen"/>
		<result column="data_expen_date1" property="dataExpenDate1"/>
		<result column="data_expen_date2" property="dataExpenDate2"/>
		<result column="data_taxdepart" property="dataTaxdepart"/>
		<result column="data_tax" property="dataTax"/>
		<result column="data_tax_date1" property="dataTaxDate1"/>
		<result column="data_tax_date2" property="dataTaxDate2"/>
	</resultMap>
	
	<update id="updateSalaryInfo" parameterType="SalaryInfo">
		UPDATE
			sal_data
		<trim prefix="SET" suffixOverrides=",">
			data_insu = #{dataInsu}
			,data_resi = #{dataResi}
			,data_pro = #{dataPro}
			<if test="dataBank != null and dataBank neq ''.toString()">
				,data_bank = #{dataBank}
			</if>
			<if test="dataAccount != null and dataAccount neq ''.toString()">
				,data_account = #{dataAccount}
			</if>
			<if test="dataHost != null and dataHost neq ''.toString()">
				,data_host = #{dataHost}
			</if>
			<if test="dataScrib != null and dataScrib neq ''.toString()">
				,data_scrib = #{dataScrib}
			</if>
			,data_bate = #{dataBate}
			<if test="dataBatePer != null and dataBatePer neq ''.toString()">
				,data_bate_per = #{dataBatePer}
			</if>
			<if test="dataBateDate1 != null and dataBateDate1 neq ''.toString()">
				,data_bate_date1 = #{dataBateDate1}
			</if>
			<if test="dataBateDate2 != null and dataBateDate2 neq ''.toString()">
				,data_bate_date2 = #{dataBateDate2}
			</if>
			,data_expen = #{dataExpen}
			<if test="dataExpenDate1 != null and dataExpenDate1 neq ''.toString()">
				,data_expen_date1 = #{dataExpenDate1}
			</if>
			<if test="dataExpenDate2 != null and dataExpenDate2 neq ''.toString()">
				,data_expen_date2 = #{dataExpenDate2}
			</if>
			,data_taxdepart = #{dataTaxdepart}
			<if test="dataTax != null and dataTax neq ''.toString()">
				,data_tax = #{dataTax}
			</if>
			<if test="dataTaxDate1 != null and dataTaxDate1 neq ''.toString()">
				,data_tax_date1 = #{dataTaxDate1}
			</if>
			<if test="dataTaxDate2 != null and dataTaxDate2 neq ''.toString()">
				,data_tax_date2 = #{dataTaxDate2}
			</if>
		</trim>
		WHERE
			data_num = #{dataNum}
	</update>
	
	<!-- 회사 정보 목록 ajax 호출을 위한 맵 타입으로 받기 -->
	<select id="salaryInfoMap" parameterType="String" resultType="Map">
		SELECT 
			data_num	AS	dataNum, 
			data_emp	AS	dataEmp,
			staff_name	AS	staffName,
			level_name	AS	levelName,
			human_num	AS	humanNum,
			company_in_date	AS	companyInDate,
			company_out_date	AS	companyOutDate,
			staff_nationality	AS	staffNationality,
			staff_country	AS	staffCountry,
			staff_phone	AS	staffPhone,
			staff_email	AS	staffEmail,
			data_insu	AS	dataInsu, 
			data_resi	AS	dataResi, 
			data_pro	AS	dataPro, 
			data_bank	AS	dataBank, 
			data_account	AS	dataAccount, 
			data_host	AS	dataHost, 
			data_scrib	AS	dataScrib, 
			data_bate	AS	dataBate, 
			data_bate_per	AS	dataBatePer, 
			data_bate_date1	AS	dataBateDate1, 
			data_bate_date2	AS	dataBateDate2, 
			data_expen	AS	dataExpen, 
			data_expen_date1	AS	dataExpenDate1, 
			data_expen_date2	AS	dataExpenDate2, 
			data_taxdepart	AS	dataTaxdepart, 
			data_tax	AS	dataTax, 
			data_tax_date1	AS	dataTaxDate1, 
			data_tax_date2	AS	dataTaxDate2
	FROM sal_data a
		INNER join
	is_staff b
		INNER join
	is_level c
		INNER join
	is_staff_basic_info d
		ON
	a.data_emp = b.staff_num
		AND
	b.staff_level_code = c.level_code
		AND
	b.staff_num = d.staff_num
		WHERE
	data_emp = #{dataEmp}
	</select>
	
	<!-- 화면 첫 로딩 시 기본정보 가장 상단 데이터 1줄 조회 -->
	<select id="salaryInfo" resultType="SalaryInfo" resultMap="SalaryInfoResultMap">
		SELECT 
			data_num, 
			data_emp,
			staff_name,
			level_name,
			human_num,
			company_in_date,
			company_out_date,
			staff_nationality,
			staff_country,
			staff_phone,
			staff_email,
			data_insu, 
			data_resi, 
			data_pro, 
			data_bank, 
			data_account, 
			data_host, 
			data_scrib, 
			data_bate, 
			data_bate_per, 
			data_bate_date1, 
			data_bate_date2, 
			data_expen, 
			data_expen_date1, 
			data_expen_date2, 
			data_taxdepart, 
			data_tax, 
			data_tax_date1, 
			data_tax_date2
	FROM sal_data a
		INNER join
	is_staff b
		INNER join
	is_level c
		INNER join
	is_staff_basic_info d
		ON
	a.data_emp = b.staff_num
		AND
	b.staff_level_code = c.level_code
		AND
	b.staff_num = d.staff_num
		limit 1
	</select>	
	
	<!-- 검색어에 따른 사원 조회 -->
	<select id="getStaffListMap" parameterType="String" resultType="Map">
		SELECT
			staff_num	AS	staffNum,
			staff_name	AS	staffName,
			level_name	AS	levelName,
			jojic_name	AS	jojicName,
			data_num	AS	dataNum
		FROM
			is_staff a
		INNER join
			is_level b
		INNER join
			is_jojic c
		INNER join
			sal_data d
		ON
			a.staff_level_code=b.level_code
		AND
			a.jojic_code=c.jojic_code
		AND
			a.staff_num=d.data_emp
		<if test="searchCate eq 1 or searchCate eq 2">
			<if test="searchText != null or searchText neq ''.toString">
				<trim prefix="WHERE">
					<if test="searchCate eq 1">
						staff_num like '%${searchText}%'
					</if>
					<if test="searchCate eq 2">
						staff_name like '%${searchText}%'
					</if>
				</trim>
			</if>
		</if>
		<if test="searchCate eq 3 or searchCate eq 4">
			<trim prefix="WHERE">
				<if test="searchCate eq 3">
					level_code like '%${searchLevel}%'
				</if>
				<if test="searchCate eq 4">
					a.jojic_code like '%${searchJojic}%'
				</if>
			</trim>
		</if>
	</select>
	
	<!-- 직급 코드 조회 -->
	<select id="getLevelList" resultType="Map">
		SELECT
			level_code,
			level_name
		FROM
			is_level
	</select>
	
	<!-- 부서 코드 조회 -->
	<select id="getJojicList" resultType="Map">
		SELECT
			jojic_code,
			jojic_name
		FROM
			is_jojic
	</select>
	
	<!-- 급상여입력 해당 연월 데이터 존재 여부 조회 -->
	<select id="getSelMonthData" parameterType="String" resultType="SalaryRecord">
		SELECT
			month_num		AS		dataNum,
			month_data		AS		dataData,
			month_date		AS		dataDate,
			month_pay		AS		dataPay,
			month_normal	AS		dataNormal,
			month_bonus		AS		dataBonus,
			month_ann		AS		dataAnn,
			month_vehicle	AS		dataVehicle,
			month_meal		AS		dataMeal,
			month_overtime	AS		dataOvertime,
			month_night		AS		dataNight,
			month_holi		AS		dataHoli,
			month_gukmin	AS		dataGukmin,
			month_geongang	AS		dataGeongang,
			month_khoyong	AS		dataKhoyong,
			month_janggi	AS		dataJanggi,
			month_sanjae	AS		dataSangae,
			month_schexpan	AS		dataSchexpan,
			month_income	AS		dataIncome,
			month_loincome	AS		dataLoincome,
			month_addpay	AS		dataAddpay,
			month_pricepay	AS		dataPricepay,
			month_sumpay	AS		dataSumpay,
			month_adddeduct	AS		dataAdddeduct,
			month_pricededuct	AS	dataPricededuct,
			month_sumdeduct	AS		dataSumdeduct
		FROM
			sal_month
		WHERE
			month_data = #{dataNum}
		AND
			right(month_date, 2) = right(#{searchDate}, 2) 
		AND 
			LEFT(month_date, 4) = LEFT(#{searchDate}, 4)
	</select>
	
	<!-- 연도 필터 월별 데이터에 존재하는 연도 조회 -->
	<select id="getFilterYear" resultType="Map">
		SELECT
			DISTINCT(LEFT(month_date, 4)) AS getYear
		FROM
			sal_month
		ORDER BY getYear DESC
	</select>
	
	<!-- 기본급여정보 사원리스트 조회 -->
	<select id="getSalaryStaffList" resultType="Map">
		SELECT
			staff_num	AS	staffNum,
			staff_name	AS	staffName,
			data_num	AS	dataNum,
			level_name	AS	levelName,
			jojic_name	AS	jojicName
		FROM
			is_staff a
		INNER join
			is_level b
		INNER join
			is_jojic c
		INNER join
			sal_data d
		ON
			a.staff_level_code=b.level_code
		AND
			a.jojic_code=c.jojic_code
		AND
			a.staff_num=d.data_emp
	</select>
	
	<select id="getMonthSalList" parameterType="String" resultType="Map">
		SELECT cate, plus, M1, M2, M3, M4, M5, M6, M7, M8, M9, M10, M11, M12 from
			(SELECT '기본급' AS 'cate', 2 AS 'join' UNION SELECT '상여',3 UNION SELECT '연차 수당',4 UNION SELECT '차량 보조',5 UNION SELECT '식대',6 UNION SELECT '잔업 수당',7 UNION SELECT '야근 수당',8 UNION SELECT '휴일 수당',9 UNION SELECT '국민 연금',10 UNION SELECT '건강 보험',11 UNION SELECT '고용 보험',12 UNION SELECT '장기요양 보험',13 UNION SELECT '산재 보험',14 UNION SELECT '학자금 공제',15 UNION SELECT '소득세',16 UNION SELECT '지방 소득세',17 UNION SELECT '부가지급 금액 합',18 UNION SELECT '부가공제 금액 합',19) AS b
		INNER JOIN
			(SELECT CASE WHEN count(*) = 0 THEN 0 ELSE SUM(month_normal) END AS 'plus', 2 AS 'join' FROM sal_month WHERE LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(*) = 0 THEN 0 ELSE SUM(month_bonus) END, 3 FROM sal_month WHERE LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(*) = 0 THEN 0 ELSE SUM(month_ann) END, 4 FROM sal_month WHERE LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(*) = 0 THEN 0 ELSE SUM(month_vehicle) END, 5 FROM sal_month WHERE LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(*) = 0 THEN 0 ELSE SUM(month_meal) END, 6 FROM sal_month WHERE LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(*) = 0 THEN 0 ELSE SUM(month_overtime) END, 7 FROM sal_month WHERE LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(*) = 0 THEN 0 ELSE SUM(month_night) END, 8 FROM sal_month WHERE LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(*) = 0 THEN 0 ELSE SUM(month_holi) END, 9 FROM sal_month WHERE LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(*) = 0 THEN 0 ELSE SUM(month_gukmin) END, 10 FROM sal_month WHERE LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(*) = 0 THEN 0 ELSE SUM(month_geongang) END, 11 FROM sal_month WHERE LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(*) = 0 THEN 0 ELSE SUM(month_khoyong) END, 12 FROM sal_month WHERE LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(*) = 0 THEN 0 ELSE SUM(month_janggi) END, 13 FROM sal_month WHERE LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(*) = 0 THEN 0 ELSE SUM(month_sanjae) END, 14 FROM sal_month WHERE LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(*) = 0 THEN 0 ELSE SUM(month_schexpan) END, 15 FROM sal_month WHERE LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(*) = 0 THEN 0 ELSE SUM(month_income) END, 16 FROM sal_month WHERE LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(*) = 0 THEN 0 ELSE SUM(month_loincome) END, 17 FROM sal_month WHERE LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(*) = 0 THEN 0 ELSE SUM(month_sumpay) END, 18 FROM sal_month WHERE LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(*) = 0 THEN 0 ELSE SUM(month_sumdeduct) END, 19 FROM sal_month WHERE LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1) AS a
		INNER JOIN
			(SELECT CASE WHEN count(month_normal) = 0 THEN 0 ELSE month_normal END AS 'M1', 2 AS 'join' from sal_month WHERE right(month_date, 2) = '01' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 union SELECT CASE WHEN count(month_bonus) = 0 THEN 0 ELSE month_bonus END , 3 from sal_month WHERE right(month_date, 2) = '01' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_ann) = 0 THEN 0 ELSE month_ann END , 4 from sal_month WHERE right(month_date, 2) = '01' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_vehicle) = 0 THEN 0 ELSE month_vehicle END , 5 from sal_month WHERE right(month_date, 2) = '01' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_meal) = 0 THEN 0 ELSE month_meal END , 6 from sal_month WHERE right(month_date, 2) = '01' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_overtime) = 0 THEN 0 ELSE month_overtime END , 7 from sal_month WHERE right(month_date, 2) = '01' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_night) = 0 THEN 0 ELSE month_night END , 8 from sal_month WHERE right(month_date, 2) = '01' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_holi) = 0 THEN 0 ELSE month_holi END , 9 from sal_month WHERE right(month_date, 2) = '01' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_gukmin) = 0 THEN 0 ELSE month_gukmin END , 10 from sal_month WHERE right(month_date, 2) = '01' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_geongang) = 0 THEN 0 ELSE month_geongang END , 11 from sal_month WHERE right(month_date, 2) = '01' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_khoyong) = 0 THEN 0 ELSE month_khoyong END , 12 from sal_month WHERE right(month_date, 2) = '01' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_janggi) = 0 THEN 0 ELSE month_janggi END , 13 from sal_month WHERE right(month_date, 2) = '01' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sanjae) = 0 THEN 0 ELSE month_sanjae END , 14 from sal_month WHERE right(month_date, 2) = '01' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_schexpan) = 0 THEN 0 ELSE month_schexpan END , 15 from sal_month WHERE right(month_date, 2) = '01' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_income) = 0 THEN 0 ELSE month_income END , 16 from sal_month WHERE right(month_date, 2) = '01' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_loincome) = 0 THEN 0 ELSE month_loincome END , 17 from sal_month WHERE right(month_date, 2) = '01' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sumpay) = 0 THEN 0 ELSE month_sumpay END , 18 from sal_month WHERE right(month_date, 2) = '01' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sumdeduct) = 0 THEN 0 ELSE month_sumdeduct END , 19 from sal_month WHERE right(month_date, 2) = '01' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1) AS c
		INNER JOIN
			(SELECT CASE WHEN count(month_normal) = 0 THEN 0 ELSE month_normal END AS 'M2', 2 AS 'join' from sal_month WHERE right(month_date, 2) = '02' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 union SELECT CASE WHEN count(month_bonus) = 0 THEN 0 ELSE month_bonus END , 3 from sal_month WHERE right(month_date, 2) = '02' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_ann) = 0 THEN 0 ELSE month_ann END , 4 from sal_month WHERE right(month_date, 2) = '02' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_vehicle) = 0 THEN 0 ELSE month_vehicle END , 5 from sal_month WHERE right(month_date, 2) = '02' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_meal) = 0 THEN 0 ELSE month_meal END , 6 from sal_month WHERE right(month_date, 2) = '02' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_overtime) = 0 THEN 0 ELSE month_overtime END , 7 from sal_month WHERE right(month_date, 2) = '02' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_night) = 0 THEN 0 ELSE month_night END , 8 from sal_month WHERE right(month_date, 2) = '02' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_holi) = 0 THEN 0 ELSE month_holi END , 9 from sal_month WHERE right(month_date, 2) = '02' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_gukmin) = 0 THEN 0 ELSE month_gukmin END , 10 from sal_month WHERE right(month_date, 2) = '02' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_geongang) = 0 THEN 0 ELSE month_geongang END , 11 from sal_month WHERE right(month_date, 2) = '02' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_khoyong) = 0 THEN 0 ELSE month_khoyong END , 12 from sal_month WHERE right(month_date, 2) = '02' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_janggi) = 0 THEN 0 ELSE month_janggi END , 13 from sal_month WHERE right(month_date, 2) = '02' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sanjae) = 0 THEN 0 ELSE month_sanjae END , 14 from sal_month WHERE right(month_date, 2) = '02' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_schexpan) = 0 THEN 0 ELSE month_schexpan END , 15 from sal_month WHERE right(month_date, 2) = '02' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_income) = 0 THEN 0 ELSE month_income END , 16 from sal_month WHERE right(month_date, 2) = '02' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_loincome) = 0 THEN 0 ELSE month_loincome END , 17 from sal_month WHERE right(month_date, 2) = '02' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sumpay) = 0 THEN 0 ELSE month_sumpay END , 18 from sal_month WHERE right(month_date, 2) = '02' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sumdeduct) = 0 THEN 0 ELSE month_sumdeduct END , 19 from sal_month WHERE right(month_date, 2) = '02' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1) AS d
		INNER JOIN
			(SELECT CASE WHEN count(month_normal) = 0 THEN 0 ELSE month_normal END AS 'M3', 2 AS 'join' from sal_month WHERE right(month_date, 2) = '03' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 union SELECT CASE WHEN count(month_bonus) = 0 THEN 0 ELSE month_bonus END , 3 from sal_month WHERE right(month_date, 2) = '03' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_ann) = 0 THEN 0 ELSE month_ann END , 4 from sal_month WHERE right(month_date, 2) = '03' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_vehicle) = 0 THEN 0 ELSE month_vehicle END , 5 from sal_month WHERE right(month_date, 2) = '03' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_meal) = 0 THEN 0 ELSE month_meal END , 6 from sal_month WHERE right(month_date, 2) = '03' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_overtime) = 0 THEN 0 ELSE month_overtime END , 7 from sal_month WHERE right(month_date, 2) = '03' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_night) = 0 THEN 0 ELSE month_night END , 8 from sal_month WHERE right(month_date, 2) = '03' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_holi) = 0 THEN 0 ELSE month_holi END , 9 from sal_month WHERE right(month_date, 2) = '03' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_gukmin) = 0 THEN 0 ELSE month_gukmin END , 10 from sal_month WHERE right(month_date, 2) = '03' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_geongang) = 0 THEN 0 ELSE month_geongang END , 11 from sal_month WHERE right(month_date, 2) = '03' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_khoyong) = 0 THEN 0 ELSE month_khoyong END , 12 from sal_month WHERE right(month_date, 2) = '03' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_janggi) = 0 THEN 0 ELSE month_janggi END , 13 from sal_month WHERE right(month_date, 2) = '03' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sanjae) = 0 THEN 0 ELSE month_sanjae END , 14 from sal_month WHERE right(month_date, 2) = '03' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_schexpan) = 0 THEN 0 ELSE month_schexpan END , 15 from sal_month WHERE right(month_date, 2) = '03' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_income) = 0 THEN 0 ELSE month_income END , 16 from sal_month WHERE right(month_date, 2) = '03' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_loincome) = 0 THEN 0 ELSE month_loincome END , 17 from sal_month WHERE right(month_date, 2) = '03' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sumpay) = 0 THEN 0 ELSE month_sumpay END , 18 from sal_month WHERE right(month_date, 2) = '03' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sumdeduct) = 0 THEN 0 ELSE month_sumdeduct END , 19 from sal_month WHERE right(month_date, 2) = '03' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1) AS e
		INNER JOIN
			(SELECT CASE WHEN count(month_normal) = 0 THEN 0 ELSE month_normal END AS 'M4', 2 AS 'join' from sal_month WHERE right(month_date, 2) = '04' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 union SELECT CASE WHEN count(month_bonus) = 0 THEN 0 ELSE month_bonus END , 3 from sal_month WHERE right(month_date, 2) = '04' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_ann) = 0 THEN 0 ELSE month_ann END , 4 from sal_month WHERE right(month_date, 2) = '04' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_vehicle) = 0 THEN 0 ELSE month_vehicle END , 5 from sal_month WHERE right(month_date, 2) = '04' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_meal) = 0 THEN 0 ELSE month_meal END , 6 from sal_month WHERE right(month_date, 2) = '04' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_overtime) = 0 THEN 0 ELSE month_overtime END , 7 from sal_month WHERE right(month_date, 2) = '04' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_night) = 0 THEN 0 ELSE month_night END , 8 from sal_month WHERE right(month_date, 2) = '04' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_holi) = 0 THEN 0 ELSE month_holi END , 9 from sal_month WHERE right(month_date, 2) = '04' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_gukmin) = 0 THEN 0 ELSE month_gukmin END , 10 from sal_month WHERE right(month_date, 2) = '04' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_geongang) = 0 THEN 0 ELSE month_geongang END , 11 from sal_month WHERE right(month_date, 2) = '04' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_khoyong) = 0 THEN 0 ELSE month_khoyong END , 12 from sal_month WHERE right(month_date, 2) = '04' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_janggi) = 0 THEN 0 ELSE month_janggi END , 13 from sal_month WHERE right(month_date, 2) = '04' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sanjae) = 0 THEN 0 ELSE month_sanjae END , 14 from sal_month WHERE right(month_date, 2) = '04' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_schexpan) = 0 THEN 0 ELSE month_schexpan END , 15 from sal_month WHERE right(month_date, 2) = '04' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_income) = 0 THEN 0 ELSE month_income END , 16 from sal_month WHERE right(month_date, 2) = '04' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_loincome) = 0 THEN 0 ELSE month_loincome END , 17 from sal_month WHERE right(month_date, 2) = '04' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sumpay) = 0 THEN 0 ELSE month_sumpay END , 18 from sal_month WHERE right(month_date, 2) = '04' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sumdeduct) = 0 THEN 0 ELSE month_sumdeduct END , 19 from sal_month WHERE right(month_date, 2) = '04' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1) AS f
		INNER JOIN
			(SELECT CASE WHEN count(month_normal) = 0 THEN 0 ELSE month_normal END AS 'M5', 2 AS 'join' from sal_month WHERE right(month_date, 2) = '05' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 union SELECT CASE WHEN count(month_bonus) = 0 THEN 0 ELSE month_bonus END , 3 from sal_month WHERE right(month_date, 2) = '05' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_ann) = 0 THEN 0 ELSE month_ann END , 4 from sal_month WHERE right(month_date, 2) = '05' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_vehicle) = 0 THEN 0 ELSE month_vehicle END , 5 from sal_month WHERE right(month_date, 2) = '05' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_meal) = 0 THEN 0 ELSE month_meal END , 6 from sal_month WHERE right(month_date, 2) = '05' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_overtime) = 0 THEN 0 ELSE month_overtime END , 7 from sal_month WHERE right(month_date, 2) = '05' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_night) = 0 THEN 0 ELSE month_night END , 8 from sal_month WHERE right(month_date, 2) = '05' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_holi) = 0 THEN 0 ELSE month_holi END , 9 from sal_month WHERE right(month_date, 2) = '05' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_gukmin) = 0 THEN 0 ELSE month_gukmin END , 10 from sal_month WHERE right(month_date, 2) = '05' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_geongang) = 0 THEN 0 ELSE month_geongang END , 11 from sal_month WHERE right(month_date, 2) = '05' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_khoyong) = 0 THEN 0 ELSE month_khoyong END , 12 from sal_month WHERE right(month_date, 2) = '05' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_janggi) = 0 THEN 0 ELSE month_janggi END , 13 from sal_month WHERE right(month_date, 2) = '05' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sanjae) = 0 THEN 0 ELSE month_sanjae END , 14 from sal_month WHERE right(month_date, 2) = '05' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_schexpan) = 0 THEN 0 ELSE month_schexpan END , 15 from sal_month WHERE right(month_date, 2) = '05' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_income) = 0 THEN 0 ELSE month_income END , 16 from sal_month WHERE right(month_date, 2) = '05' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_loincome) = 0 THEN 0 ELSE month_loincome END , 17 from sal_month WHERE right(month_date, 2) = '05' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sumpay) = 0 THEN 0 ELSE month_sumpay END , 18 from sal_month WHERE right(month_date, 2) = '05' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sumdeduct) = 0 THEN 0 ELSE month_sumdeduct END , 19 from sal_month WHERE right(month_date, 2) = '05' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1) AS g
		INNER JOIN
			(SELECT CASE WHEN count(month_normal) = 0 THEN 0 ELSE month_normal END AS 'M6', 2 AS 'join' from sal_month WHERE right(month_date, 2) = '06' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 union SELECT CASE WHEN count(month_bonus) = 0 THEN 0 ELSE month_bonus END , 3 from sal_month WHERE right(month_date, 2) = '06' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_ann) = 0 THEN 0 ELSE month_ann END , 4 from sal_month WHERE right(month_date, 2) = '06' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_vehicle) = 0 THEN 0 ELSE month_vehicle END , 5 from sal_month WHERE right(month_date, 2) = '06' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_meal) = 0 THEN 0 ELSE month_meal END , 6 from sal_month WHERE right(month_date, 2) = '06' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_overtime) = 0 THEN 0 ELSE month_overtime END , 7 from sal_month WHERE right(month_date, 2) = '06' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_night) = 0 THEN 0 ELSE month_night END , 8 from sal_month WHERE right(month_date, 2) = '06' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_holi) = 0 THEN 0 ELSE month_holi END , 9 from sal_month WHERE right(month_date, 2) = '06' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_gukmin) = 0 THEN 0 ELSE month_gukmin END , 10 from sal_month WHERE right(month_date, 2) = '06' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_geongang) = 0 THEN 0 ELSE month_geongang END , 11 from sal_month WHERE right(month_date, 2) = '06' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_khoyong) = 0 THEN 0 ELSE month_khoyong END , 12 from sal_month WHERE right(month_date, 2) = '06' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_janggi) = 0 THEN 0 ELSE month_janggi END , 13 from sal_month WHERE right(month_date, 2) = '06' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sanjae) = 0 THEN 0 ELSE month_sanjae END , 14 from sal_month WHERE right(month_date, 2) = '06' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_schexpan) = 0 THEN 0 ELSE month_schexpan END , 15 from sal_month WHERE right(month_date, 2) = '06' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_income) = 0 THEN 0 ELSE month_income END , 16 from sal_month WHERE right(month_date, 2) = '06' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_loincome) = 0 THEN 0 ELSE month_loincome END , 17 from sal_month WHERE right(month_date, 2) = '06' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sumpay) = 0 THEN 0 ELSE month_sumpay END , 18 from sal_month WHERE right(month_date, 2) = '06' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sumdeduct) = 0 THEN 0 ELSE month_sumdeduct END , 19 from sal_month WHERE right(month_date, 2) = '06' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1) AS h
		INNER JOIN
			(SELECT CASE WHEN count(month_normal) = 0 THEN 0 ELSE month_normal END AS 'M7', 2 AS 'join' from sal_month WHERE right(month_date, 2) = '07' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 union SELECT CASE WHEN count(month_bonus) = 0 THEN 0 ELSE month_bonus END , 3 from sal_month WHERE right(month_date, 2) = '07' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_ann) = 0 THEN 0 ELSE month_ann END , 4 from sal_month WHERE right(month_date, 2) = '07' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_vehicle) = 0 THEN 0 ELSE month_vehicle END , 5 from sal_month WHERE right(month_date, 2) = '07' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_meal) = 0 THEN 0 ELSE month_meal END , 6 from sal_month WHERE right(month_date, 2) = '07' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_overtime) = 0 THEN 0 ELSE month_overtime END , 7 from sal_month WHERE right(month_date, 2) = '07' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_night) = 0 THEN 0 ELSE month_night END , 8 from sal_month WHERE right(month_date, 2) = '07' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_holi) = 0 THEN 0 ELSE month_holi END , 9 from sal_month WHERE right(month_date, 2) = '07' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_gukmin) = 0 THEN 0 ELSE month_gukmin END , 10 from sal_month WHERE right(month_date, 2) = '07' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_geongang) = 0 THEN 0 ELSE month_geongang END , 11 from sal_month WHERE right(month_date, 2) = '07' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_khoyong) = 0 THEN 0 ELSE month_khoyong END , 12 from sal_month WHERE right(month_date, 2) = '07' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_janggi) = 0 THEN 0 ELSE month_janggi END , 13 from sal_month WHERE right(month_date, 2) = '07' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sanjae) = 0 THEN 0 ELSE month_sanjae END , 14 from sal_month WHERE right(month_date, 2) = '07' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_schexpan) = 0 THEN 0 ELSE month_schexpan END , 15 from sal_month WHERE right(month_date, 2) = '07' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_income) = 0 THEN 0 ELSE month_income END , 16 from sal_month WHERE right(month_date, 2) = '07' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_loincome) = 0 THEN 0 ELSE month_loincome END , 17 from sal_month WHERE right(month_date, 2) = '07' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sumpay) = 0 THEN 0 ELSE month_sumpay END , 18 from sal_month WHERE right(month_date, 2) = '07' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sumdeduct) = 0 THEN 0 ELSE month_sumdeduct END , 19 from sal_month WHERE right(month_date, 2) = '07' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1) AS i
		INNER JOIN
			(SELECT CASE WHEN count(month_normal) = 0 THEN 0 ELSE month_normal END AS 'M8', 2 AS 'join' from sal_month WHERE right(month_date, 2) = '08' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 union SELECT CASE WHEN count(month_bonus) = 0 THEN 0 ELSE month_bonus END , 3 from sal_month WHERE right(month_date, 2) = '08' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_ann) = 0 THEN 0 ELSE month_ann END , 4 from sal_month WHERE right(month_date, 2) = '08' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_vehicle) = 0 THEN 0 ELSE month_vehicle END , 5 from sal_month WHERE right(month_date, 2) = '08' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_meal) = 0 THEN 0 ELSE month_meal END , 6 from sal_month WHERE right(month_date, 2) = '08' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_overtime) = 0 THEN 0 ELSE month_overtime END , 7 from sal_month WHERE right(month_date, 2) = '08' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_night) = 0 THEN 0 ELSE month_night END , 8 from sal_month WHERE right(month_date, 2) = '08' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_holi) = 0 THEN 0 ELSE month_holi END , 9 from sal_month WHERE right(month_date, 2) = '08' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_gukmin) = 0 THEN 0 ELSE month_gukmin END , 10 from sal_month WHERE right(month_date, 2) = '08' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_geongang) = 0 THEN 0 ELSE month_geongang END , 11 from sal_month WHERE right(month_date, 2) = '08' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_khoyong) = 0 THEN 0 ELSE month_khoyong END , 12 from sal_month WHERE right(month_date, 2) = '08' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_janggi) = 0 THEN 0 ELSE month_janggi END , 13 from sal_month WHERE right(month_date, 2) = '08' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sanjae) = 0 THEN 0 ELSE month_sanjae END , 14 from sal_month WHERE right(month_date, 2) = '08' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_schexpan) = 0 THEN 0 ELSE month_schexpan END , 15 from sal_month WHERE right(month_date, 2) = '08' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_income) = 0 THEN 0 ELSE month_income END , 16 from sal_month WHERE right(month_date, 2) = '08' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_loincome) = 0 THEN 0 ELSE month_loincome END , 17 from sal_month WHERE right(month_date, 2) = '08' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sumpay) = 0 THEN 0 ELSE month_sumpay END , 18 from sal_month WHERE right(month_date, 2) = '08' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sumdeduct) = 0 THEN 0 ELSE month_sumdeduct END , 19 from sal_month WHERE right(month_date, 2) = '08' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1) AS j
		INNER JOIN
			(SELECT CASE WHEN count(month_normal) = 0 THEN 0 ELSE month_normal END AS 'M9', 2 AS 'join' from sal_month WHERE right(month_date, 2) = '09' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 union SELECT CASE WHEN count(month_bonus) = 0 THEN 0 ELSE month_bonus END , 3 from sal_month WHERE right(month_date, 2) = '09' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_ann) = 0 THEN 0 ELSE month_ann END , 4 from sal_month WHERE right(month_date, 2) = '09' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_vehicle) = 0 THEN 0 ELSE month_vehicle END , 5 from sal_month WHERE right(month_date, 2) = '09' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_meal) = 0 THEN 0 ELSE month_meal END , 6 from sal_month WHERE right(month_date, 2) = '09' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_overtime) = 0 THEN 0 ELSE month_overtime END , 7 from sal_month WHERE right(month_date, 2) = '09' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_night) = 0 THEN 0 ELSE month_night END , 8 from sal_month WHERE right(month_date, 2) = '09' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_holi) = 0 THEN 0 ELSE month_holi END , 9 from sal_month WHERE right(month_date, 2) = '09' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_gukmin) = 0 THEN 0 ELSE month_gukmin END , 10 from sal_month WHERE right(month_date, 2) = '09' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_geongang) = 0 THEN 0 ELSE month_geongang END , 11 from sal_month WHERE right(month_date, 2) = '09' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_khoyong) = 0 THEN 0 ELSE month_khoyong END , 12 from sal_month WHERE right(month_date, 2) = '09' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_janggi) = 0 THEN 0 ELSE month_janggi END , 13 from sal_month WHERE right(month_date, 2) = '09' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sanjae) = 0 THEN 0 ELSE month_sanjae END , 14 from sal_month WHERE right(month_date, 2) = '09' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_schexpan) = 0 THEN 0 ELSE month_schexpan END , 15 from sal_month WHERE right(month_date, 2) = '09' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_income) = 0 THEN 0 ELSE month_income END , 16 from sal_month WHERE right(month_date, 2) = '09' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_loincome) = 0 THEN 0 ELSE month_loincome END , 17 from sal_month WHERE right(month_date, 2) = '09' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sumpay) = 0 THEN 0 ELSE month_sumpay END , 18 from sal_month WHERE right(month_date, 2) = '09' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sumdeduct) = 0 THEN 0 ELSE month_sumdeduct END , 19 from sal_month WHERE right(month_date, 2) = '09' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1) AS k
		INNER JOIN
			(SELECT CASE WHEN count(month_normal) = 0 THEN 0 ELSE month_normal END AS 'M10', 2 AS 'join' from sal_month WHERE right(month_date, 2) = '10' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 union SELECT CASE WHEN count(month_bonus) = 0 THEN 0 ELSE month_bonus END , 3 from sal_month WHERE right(month_date, 2) = '10' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_ann) = 0 THEN 0 ELSE month_ann END , 4 from sal_month WHERE right(month_date, 2) = '10' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_vehicle) = 0 THEN 0 ELSE month_vehicle END , 5 from sal_month WHERE right(month_date, 2) = '10' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_meal) = 0 THEN 0 ELSE month_meal END , 6 from sal_month WHERE right(month_date, 2) = '10' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_overtime) = 0 THEN 0 ELSE month_overtime END , 7 from sal_month WHERE right(month_date, 2) = '10' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_night) = 0 THEN 0 ELSE month_night END , 8 from sal_month WHERE right(month_date, 2) = '10' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_holi) = 0 THEN 0 ELSE month_holi END , 9 from sal_month WHERE right(month_date, 2) = '10' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_gukmin) = 0 THEN 0 ELSE month_gukmin END , 10 from sal_month WHERE right(month_date, 2) = '10' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_geongang) = 0 THEN 0 ELSE month_geongang END , 11 from sal_month WHERE right(month_date, 2) = '10' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_khoyong) = 0 THEN 0 ELSE month_khoyong END , 12 from sal_month WHERE right(month_date, 2) = '10' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_janggi) = 0 THEN 0 ELSE month_janggi END , 13 from sal_month WHERE right(month_date, 2) = '10' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sanjae) = 0 THEN 0 ELSE month_sanjae END , 14 from sal_month WHERE right(month_date, 2) = '10' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_schexpan) = 0 THEN 0 ELSE month_schexpan END , 15 from sal_month WHERE right(month_date, 2) = '10' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_income) = 0 THEN 0 ELSE month_income END , 16 from sal_month WHERE right(month_date, 2) = '10' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_loincome) = 0 THEN 0 ELSE month_loincome END , 17 from sal_month WHERE right(month_date, 2) = '10' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sumpay) = 0 THEN 0 ELSE month_sumpay END , 18 from sal_month WHERE right(month_date, 2) = '10' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sumdeduct) = 0 THEN 0 ELSE month_sumdeduct END , 19 from sal_month WHERE right(month_date, 2) = '10' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1) AS l
		INNER JOIN
			(SELECT CASE WHEN count(month_normal) = 0 THEN 0 ELSE month_normal END AS 'M11', 2 AS 'join' from sal_month WHERE right(month_date, 2) = '11' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 union SELECT CASE WHEN count(month_bonus) = 0 THEN 0 ELSE month_bonus END , 3 from sal_month WHERE right(month_date, 2) = '11' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_ann) = 0 THEN 0 ELSE month_ann END , 4 from sal_month WHERE right(month_date, 2) = '11' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_vehicle) = 0 THEN 0 ELSE month_vehicle END , 5 from sal_month WHERE right(month_date, 2) = '11' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_meal) = 0 THEN 0 ELSE month_meal END , 6 from sal_month WHERE right(month_date, 2) = '11' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_overtime) = 0 THEN 0 ELSE month_overtime END , 7 from sal_month WHERE right(month_date, 2) = '11' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_night) = 0 THEN 0 ELSE month_night END , 8 from sal_month WHERE right(month_date, 2) = '11' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_holi) = 0 THEN 0 ELSE month_holi END , 9 from sal_month WHERE right(month_date, 2) = '11' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_gukmin) = 0 THEN 0 ELSE month_gukmin END , 10 from sal_month WHERE right(month_date, 2) = '11' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_geongang) = 0 THEN 0 ELSE month_geongang END , 11 from sal_month WHERE right(month_date, 2) = '11' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_khoyong) = 0 THEN 0 ELSE month_khoyong END , 12 from sal_month WHERE right(month_date, 2) = '11' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_janggi) = 0 THEN 0 ELSE month_janggi END , 13 from sal_month WHERE right(month_date, 2) = '11' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sanjae) = 0 THEN 0 ELSE month_sanjae END , 14 from sal_month WHERE right(month_date, 2) = '11' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_schexpan) = 0 THEN 0 ELSE month_schexpan END , 15 from sal_month WHERE right(month_date, 2) = '11' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_income) = 0 THEN 0 ELSE month_income END , 16 from sal_month WHERE right(month_date, 2) = '11' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_loincome) = 0 THEN 0 ELSE month_loincome END , 17 from sal_month WHERE right(month_date, 2) = '11' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sumpay) = 0 THEN 0 ELSE month_sumpay END , 18 from sal_month WHERE right(month_date, 2) = '11' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sumdeduct) = 0 THEN 0 ELSE month_sumdeduct END , 19 from sal_month WHERE right(month_date, 2) = '11' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1) AS m
		INNER JOIN
			(SELECT CASE WHEN count(month_normal) = 0 THEN 0 ELSE month_normal END AS 'M12', 2 AS 'join' from sal_month WHERE right(month_date, 2) = '12' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 union SELECT CASE WHEN count(month_bonus) = 0 THEN 0 ELSE month_bonus END , 3 from sal_month WHERE right(month_date, 2) = '12' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_ann) = 0 THEN 0 ELSE month_ann END , 4 from sal_month WHERE right(month_date, 2) = '12' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_vehicle) = 0 THEN 0 ELSE month_vehicle END , 5 from sal_month WHERE right(month_date, 2) = '12' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_meal) = 0 THEN 0 ELSE month_meal END , 6 from sal_month WHERE right(month_date, 2) = '12' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_overtime) = 0 THEN 0 ELSE month_overtime END , 7 from sal_month WHERE right(month_date, 2) = '12' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_night) = 0 THEN 0 ELSE month_night END , 8 from sal_month WHERE right(month_date, 2) = '12' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_holi) = 0 THEN 0 ELSE month_holi END , 9 from sal_month WHERE right(month_date, 2) = '12' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_gukmin) = 0 THEN 0 ELSE month_gukmin END , 10 from sal_month WHERE right(month_date, 2) = '12' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_geongang) = 0 THEN 0 ELSE month_geongang END , 11 from sal_month WHERE right(month_date, 2) = '12' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_khoyong) = 0 THEN 0 ELSE month_khoyong END , 12 from sal_month WHERE right(month_date, 2) = '12' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_janggi) = 0 THEN 0 ELSE month_janggi END , 13 from sal_month WHERE right(month_date, 2) = '12' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sanjae) = 0 THEN 0 ELSE month_sanjae END , 14 from sal_month WHERE right(month_date, 2) = '12' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_schexpan) = 0 THEN 0 ELSE month_schexpan END , 15 from sal_month WHERE right(month_date, 2) = '12' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_income) = 0 THEN 0 ELSE month_income END , 16 from sal_month WHERE right(month_date, 2) = '12' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_loincome) = 0 THEN 0 ELSE month_loincome END , 17 from sal_month WHERE right(month_date, 2) = '12' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sumpay) = 0 THEN 0 ELSE month_sumpay END , 18 from sal_month WHERE right(month_date, 2) = '12' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1 UNION SELECT CASE WHEN count(month_sumdeduct) = 0 THEN 0 ELSE month_sumdeduct END , 19 from sal_month WHERE right(month_date, 2) = '12' AND LEFT(month_date, 4) = #{searchYear} AND month_data = #{dataNum} AND month_pay = 1) AS n
		ON
			b.join = a.JOIN
		AND
			b.join = c.join
		AND
			b.join = d.join
		AND
			b.join = e.join
		AND
			b.join = f.join
		AND
			b.join = g.join
		AND
			b.join = h.join
		AND
			b.join = i.join
		AND
			b.join = j.join
		AND
			b.join = k.join
		AND
			b.join = l.join
		AND
			b.join = m.join
		AND
			b.join = n.join
	</select>
</mapper>	
